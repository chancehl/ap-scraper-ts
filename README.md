```
Usage: index [options]

Options:
  -o, --outdir <outdir>        the directory to save the images to (default: "./imgs")
  -r, --reportDir <reportDir>  the directory to save the report to (default: "./report")
  -n, --numImages <numImages>  the maximum number of images to scrape
  -d, --debug                  puts the program into debug mode which enables more verbose logging and screenshots on failure (default: false)
  -q, --quiet                  disables console logging (default: false)
  -h, --help                   display help for command
```
